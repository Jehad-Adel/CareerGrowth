import { PageStub } from "@/components/layout/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Dashboard"
      description="Your career at a glance — level, progress, and next actions."
    />
  );
}
