# Dependencies of the `ai/` package (LangChain + Google Gemini).
#
# Temporary: `ai/` is being folded into `backend/app/` behind the services
# layer, after which these move to backend/pyproject.toml and this file goes
# away. Keep it in sync with actual imports — nothing unused belongs here.

langchain-core>=0.3
langchain-google-genai>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
pypdf>=5.0
